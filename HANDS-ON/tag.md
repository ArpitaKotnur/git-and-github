# why tags?
tag for the commit so you can easily identify and use it as release
# types of tag
- annoted tag - more informative
- lightweight tag
to give tag for commit you did
```bash
git tag -a TAG_NAME -m "for the commit"
git tag
```
lightweight tag
```bash
git tag TAG_NAME
```
